package io.confluent.kafka.schemaregistry.test.categories;

/**
 * JUnit @Category that marks unsupported by MapR tests
 * which are ignored by both surefire and failsafe
 */
public interface UnsupportedTest {
}
